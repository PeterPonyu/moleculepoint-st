# Isolated baseline runners

These scripts are outside `src/`, `tests/`, and `scripts/` so upstream names/imports stay out of executable track code scanned by the brand gate. They write generic artifacts consumed for META verification.
